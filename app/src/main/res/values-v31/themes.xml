<resources>
    <!-- Android 12+ draws its own system splash; point it at the same color. -->
    <style name="Theme.SnapGPS" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/ic_launcher_background</item>
        <item name="android:windowSplashScreenBackground">@color/ic_launcher_background</item>
    </style>
</resources>
